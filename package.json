{
  "name": "notes",
  "version": "1.0.0",
  "description": "Documentation for all the things I might forget.",
  "author": "Samuel Davis &lt;samueljakdavis@gmail.com&gt;",
  "private": true,
  "scripts": {
    "dev": "nuxt --hostname=0.0.0.0",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "highlight.js": "^10.4.1",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "node-sass": "^4.13.0",
    "push-dir": "^0.4.1",
    "sass-loader": "^8.0.1"
  }
}
