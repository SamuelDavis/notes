import NotesPaper from './NotesPaper.vue'

// Explicitly name components, rather than just using their `name` property
// so that Intellij IDE can correctly infer which component I'm referencing in templates
export default {
  'notes-paper': NotesPaper
}
